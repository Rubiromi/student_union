<h1>Student List</h1>

<ul>
  <% @students.each do |student| %>
    <li><%= student.name %> | <%= student.course %> <%= link_to 'Delete', student, method: :delete %> <%= link_to 'Edit', student, method: :edit %></li>
  <% end %>
</ul>

<%= link_to 'Add a new student', new_student_path %>
