<%= form_for(@student) do |f| %>
  <div>
    <%= f.label(:name) %>
    <%= f.text_field(:name) %>
  </div>

  <div>
    <%= f.label(:course) %>
    <%= f.text_field(:course) %>
  </div>
  
  <div>
    <%= f.submit('Save Student') %>
  </div>
<% end %>
